import React from 'react';

export const metadata = {
  title: "KeenKeeper | Stats Page",
};

const Stats = () => {
    return (
        <div>
            Stats
        </div>
    );
};

export default Stats;